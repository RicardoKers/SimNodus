# Examples and teaching projects

No executable examples exist yet. Initial candidates are GPIO with external LED/RC, button/EXTI, and ADC with UART.

Every future example must include circuit sources, firmware sources/build recipe, model provenance, tested versions, expected traces, known limitations, and English instructions. Ignore generated ELF/build output by default; deliberately published test artifacts require provenance and review.
