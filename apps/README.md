# Application entry points

Planned `cli/`: headless backend experiments and reproducible simulation runs. Planned `desktop/`: Qt 6 circuit editor and teaching instruments.

Neither application exists yet. The CLI comes first; the desktop consumes the same application/domain contracts. Do not place solver ownership or a second scheduler inside the GUI.
