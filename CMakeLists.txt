cmake_minimum_required(VERSION 3.24)

project(SimNodus VERSION 0.0.0 DESCRIPTION "Mixed-signal and embedded systems simulator" LANGUAGES CXX)

# This foundation does not build a simulator or download runtime dependencies.
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)

add_library(simnodus_build_options INTERFACE)
add_library(simnodus::build_options ALIAS simnodus_build_options)
target_compile_features(simnodus_build_options INTERFACE cxx_std_20)

find_package(Python3 3.10 REQUIRED COMPONENTS Interpreter)
add_custom_target(check-repository
    COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/tools/check_repository.py"
    WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
    COMMENT "Checking repository documentation and structure"
    VERBATIM
)

include(CTest)
add_library(simnodus_declaration_ingress STATIC src/application/declaration_ingress.cpp)
add_library(simnodus_lock STATIC src/application/lock_validation.cpp src/application/lock_digest.cpp)
target_include_directories(simnodus_lock PUBLIC src)
target_link_libraries(simnodus_lock PUBLIC simnodus_declaration_ingress PRIVATE simnodus::build_options)
if(MSVC)
    target_compile_options(simnodus_lock PRIVATE /W4 /WX /permissive- /EHsc)
endif()
if(BUILD_TESTING)
    add_executable(native_lock_tests tests/schema/native_lock.cpp)
    target_link_libraries(native_lock_tests PRIVATE simnodus_lock)
    add_test(NAME native-lock-contracts COMMAND native_lock_tests)
    add_executable(native_lock_probe tests/schema/native_lock_probe.cpp)
    target_link_libraries(native_lock_probe PRIVATE simnodus_lock)
    add_test(NAME native-lock-differential
        COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/tests/schema/native_lock_regression.py"
            --probe "$<TARGET_FILE:native_lock_probe>")
    set_tests_properties(native-lock-differential PROPERTIES WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}" TIMEOUT 120)
endif()
add_library(simnodus_topology STATIC src/application/topology_validation.cpp)
add_library(simnodus_parameters STATIC src/application/parameter_validation.cpp)
add_library(simnodus_resource_links STATIC src/application/resource_links.cpp)
add_library(simnodus_project STATIC src/application/project_validation.cpp)
add_library(simnodus_project_graph STATIC src/application/project_graph.cpp)
target_include_directories(simnodus_project_graph PUBLIC src)
target_link_libraries(simnodus_project_graph PUBLIC simnodus_project PRIVATE simnodus::build_options)
if(MSVC)
    target_compile_options(simnodus_project_graph PRIVATE /W4 /WX /permissive- /EHsc)
endif()
if(BUILD_TESTING)
    add_executable(native_graph_tests tests/schema/native_graph.cpp)
    target_link_libraries(native_graph_tests PRIVATE simnodus_project_graph)
    add_test(NAME native-source-graph-contracts COMMAND native_graph_tests "${CMAKE_CURRENT_SOURCE_DIR}/tests/schema/fixtures/two-rc-project.json")
    add_executable(native_graph_probe tests/schema/native_graph_probe.cpp)
    target_link_libraries(native_graph_probe PRIVATE simnodus_project_graph)
    add_test(NAME native-source-graph-differential COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/tests/schema/native_graph_regression.py" --probe "$<TARGET_FILE:native_graph_probe>")
    set_tests_properties(native-source-graph-differential PROPERTIES WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}" TIMEOUT 120)
endif()
target_include_directories(simnodus_project PUBLIC src)
target_link_libraries(simnodus_project PUBLIC simnodus_resource_links PRIVATE simnodus::build_options)
if(MSVC)
    target_compile_options(simnodus_project PRIVATE /W4 /WX /permissive- /EHsc)
endif()
if(BUILD_TESTING)
    add_executable(native_project_tests tests/schema/native_project.cpp)
    target_link_libraries(native_project_tests PRIVATE simnodus_project)
    add_test(NAME native-project-contracts COMMAND native_project_tests "${CMAKE_CURRENT_SOURCE_DIR}/tests/schema/fixtures/two-rc-project.json")
    add_executable(native_project_probe tests/schema/native_project_probe.cpp)
    target_link_libraries(native_project_probe PRIVATE simnodus_project)
    add_test(NAME native-project-differential COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/tests/schema/native_project_regression.py" --probe "$<TARGET_FILE:native_project_probe>")
    set_tests_properties(native-project-differential PROPERTIES WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}" TIMEOUT 120)
endif()
target_include_directories(simnodus_resource_links PUBLIC src)
target_link_libraries(simnodus_resource_links PUBLIC simnodus_parameters simnodus_lock PRIVATE simnodus::build_options)
if(MSVC)
    target_compile_options(simnodus_resource_links PRIVATE /W4 /WX /permissive- /EHsc)
endif()
if(BUILD_TESTING)
    add_executable(native_links_tests tests/schema/native_links.cpp)
    target_link_libraries(native_links_tests PRIVATE simnodus_resource_links)
    add_test(NAME native-resource-link-contracts COMMAND native_links_tests "${CMAKE_CURRENT_SOURCE_DIR}/tests/schema/fixtures/two-rc-resource-links.json")
    add_executable(native_links_probe tests/schema/native_links_probe.cpp)
    target_link_libraries(native_links_probe PRIVATE simnodus_resource_links)
    add_test(NAME native-resource-link-differential COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/tests/schema/native_links_regression.py" --probe "$<TARGET_FILE:native_links_probe>")
    set_tests_properties(native-resource-link-differential PROPERTIES WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}" TIMEOUT 120)
endif()
target_include_directories(simnodus_parameters PUBLIC src)
target_link_libraries(simnodus_parameters PUBLIC simnodus_topology PRIVATE simnodus::build_options)
if(MSVC)
    target_compile_options(simnodus_parameters PRIVATE /W4 /WX /permissive- /EHsc)
endif()
if(BUILD_TESTING)
    add_executable(native_bindings_tests tests/schema/native_bindings.cpp)
    target_link_libraries(native_bindings_tests PRIVATE simnodus_parameters)
    add_test(NAME native-binding-contracts COMMAND native_bindings_tests)
    add_executable(native_bindings_probe tests/schema/native_bindings_probe.cpp)
    target_link_libraries(native_bindings_probe PRIVATE simnodus_parameters)
    add_test(NAME native-binding-differential
        COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/tests/schema/native_bindings_regression.py"
            --probe "$<TARGET_FILE:native_bindings_probe>")
    set_tests_properties(native-binding-differential PROPERTIES WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}" TIMEOUT 120)
    add_executable(native_parameters_tests tests/schema/native_parameters.cpp)
    target_link_libraries(native_parameters_tests PRIVATE simnodus_parameters)
    add_test(NAME native-parameter-contracts COMMAND native_parameters_tests)
    add_executable(native_parameters_probe tests/schema/native_parameters_probe.cpp)
    target_link_libraries(native_parameters_probe PRIVATE simnodus_parameters)
    add_test(NAME native-parameter-differential
        COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/tests/schema/native_parameters_regression.py"
            --probe "$<TARGET_FILE:native_parameters_probe>")
    set_tests_properties(native-parameter-differential PROPERTIES WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}" TIMEOUT 120)
endif()
target_include_directories(simnodus_topology PUBLIC src)
target_link_libraries(simnodus_topology PUBLIC simnodus_declaration_ingress PRIVATE simnodus::build_options)
if(MSVC)
    target_compile_options(simnodus_topology PRIVATE /W4 /WX /permissive- /EHsc)
endif()
if(BUILD_TESTING)
    add_executable(native_topology_tests tests/schema/native_topology.cpp)
    target_link_libraries(native_topology_tests PRIVATE simnodus_topology)
    add_test(NAME native-topology-contracts COMMAND native_topology_tests)
    add_executable(native_topology_probe tests/schema/native_topology_probe.cpp)
    target_link_libraries(native_topology_probe PRIVATE simnodus_topology)
    add_test(NAME native-topology-differential
        COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/tests/schema/native_topology_regression.py"
            --probe "$<TARGET_FILE:native_topology_probe>")
    set_tests_properties(native-topology-differential PROPERTIES WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}" TIMEOUT 120)
endif()
target_include_directories(simnodus_declaration_ingress PUBLIC src)
target_link_libraries(simnodus_declaration_ingress PRIVATE simnodus::build_options)
if(MSVC)
    target_compile_options(simnodus_declaration_ingress PRIVATE /W4 /WX /permissive- /EHsc)
endif()
if(BUILD_TESTING)
    add_executable(native_ingress_tests tests/schema/native_ingress.cpp)
    target_link_libraries(native_ingress_tests PRIVATE simnodus_declaration_ingress)
    add_test(NAME native-declaration-ingress COMMAND native_ingress_tests)
    add_executable(native_ingress_probe tests/schema/native_ingress_probe.cpp)
    target_link_libraries(native_ingress_probe PRIVATE simnodus_declaration_ingress)
    add_test(NAME native-ingress-differential
        COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/tests/schema/native_ingress_regression.py"
            --probe "$<TARGET_FILE:native_ingress_probe>")
    set_tests_properties(native-ingress-differential PROPERTIES WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}" TIMEOUT 120)
endif()
add_library(simnodus_resources STATIC src/application/local_resources.cpp)
add_library(simnodus_project_revision STATIC src/application/project_revision.cpp)
target_include_directories(simnodus_project_revision PUBLIC src)
target_link_libraries(simnodus_project_revision PUBLIC simnodus_project_graph PRIVATE simnodus::build_options)
if(MSVC)
    target_compile_options(simnodus_project_revision PRIVATE /W4 /WX /permissive- /EHsc)
endif()
if(BUILD_TESTING)
    add_executable(native_revision_probe tests/schema/native_revision_probe.cpp)
    target_link_libraries(native_revision_probe PRIVATE simnodus_project_revision)
    add_test(NAME native-project-revision COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/tests/schema/native_revision_regression.py" --probe "$<TARGET_FILE:native_revision_probe>")
    set_tests_properties(native-project-revision PROPERTIES WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}" TIMEOUT 180)
endif()
add_library(simnodus_project_acquisition STATIC src/application/project_acquisition.cpp)
target_include_directories(simnodus_project_acquisition PUBLIC src)
target_link_libraries(simnodus_project_acquisition PUBLIC simnodus_project_graph PRIVATE simnodus_resources simnodus::build_options)
if(WIN32)
    target_sources(simnodus_project_acquisition PRIVATE src/platform/windows/project_acquisition.cpp)
    target_compile_definitions(simnodus_project_acquisition PRIVATE WIN32_LEAN_AND_MEAN NOMINMAX _WIN32_WINNT=0x0A00)
    target_link_libraries(simnodus_project_acquisition PRIVATE ntdll)
endif()
if(MSVC)
    target_compile_options(simnodus_project_acquisition PRIVATE /W4 /WX /permissive- /EHsc)
endif()
if(BUILD_TESTING)
    add_executable(native_acquisition_probe tests/resources/native_acquisition_probe.cpp)
    target_link_libraries(native_acquisition_probe PRIVATE simnodus_project_acquisition)
    set(acquisition_test_hooks)
    if(WIN32)
        add_executable(native_acquisition_hook_probe tests/resources/native_acquisition_probe.cpp src/application/project_acquisition.cpp src/platform/windows/project_acquisition.cpp)
        target_link_libraries(native_acquisition_hook_probe PRIVATE simnodus_project_graph simnodus_resources simnodus::build_options ntdll)
        target_compile_definitions(native_acquisition_hook_probe PRIVATE SIMNODUS_ACQUISITION_TEST_HOOKS WIN32_LEAN_AND_MEAN NOMINMAX _WIN32_WINNT=0x0A00)
        if(MSVC)
            target_compile_options(native_acquisition_hook_probe PRIVATE /W4 /WX /permissive- /EHsc)
        endif()
        list(APPEND acquisition_test_hooks --hooks "$<TARGET_FILE:native_acquisition_hook_probe>")
    endif()
    add_test(NAME native-project-acquisition COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/tests/resources/native_acquisition_regression.py" --probe "$<TARGET_FILE:native_acquisition_probe>" ${acquisition_test_hooks})
    set_tests_properties(native-project-acquisition PROPERTIES WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}" TIMEOUT 180)
endif()
add_library(simnodus_project_save STATIC src/application/project_save.cpp)
target_include_directories(simnodus_project_save PUBLIC src)
target_link_libraries(simnodus_project_save PUBLIC simnodus_project PRIVATE simnodus_resources simnodus::build_options)
if(WIN32)
    target_sources(simnodus_project_save PRIVATE src/platform/windows/project_save.cpp)
    target_compile_definitions(simnodus_project_save PRIVATE WIN32_LEAN_AND_MEAN NOMINMAX _WIN32_WINNT=0x0A00)
    target_link_libraries(simnodus_project_save PRIVATE bcrypt ntdll)
endif()
if(MSVC)
    target_compile_options(simnodus_project_save PRIVATE /W4 /WX /permissive- /EHsc)
endif()
if(BUILD_TESTING)
    add_executable(native_save_contract_tests tests/resources/native_save_contracts.cpp)
    target_link_libraries(native_save_contract_tests PRIVATE simnodus_project_save)
    add_test(NAME native-save-contracts COMMAND native_save_contract_tests "${CMAKE_CURRENT_SOURCE_DIR}/tests/schema/fixtures/two-rc-project.json")
    if(WIN32)
        add_executable(native_save_probe tests/resources/native_save_probe.cpp)
        target_link_libraries(native_save_probe PRIVATE simnodus_project_save)
        add_executable(native_save_hook_probe tests/resources/native_save_probe.cpp src/application/project_save.cpp src/platform/windows/project_save.cpp)
        target_link_libraries(native_save_hook_probe PRIVATE simnodus_project simnodus_resources simnodus::build_options bcrypt ntdll)
        target_compile_definitions(native_save_hook_probe PRIVATE SIMNODUS_SAVE_TEST_HOOKS WIN32_LEAN_AND_MEAN NOMINMAX _WIN32_WINNT=0x0A00)
        if(MSVC)
            target_compile_options(native_save_hook_probe PRIVATE /W4 /WX /permissive- /EHsc)
        endif()
        add_test(NAME native-save-filesystem COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/tests/resources/native_save_regression.py" --probe "$<TARGET_FILE:native_save_probe>" --hooks "$<TARGET_FILE:native_save_hook_probe>")
        set_tests_properties(native-save-filesystem PROPERTIES WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}" TIMEOUT 180)
        add_test(NAME windows-overwrite-boundary COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/tests/resources/windows_overwrite_boundary.py")
        set_tests_properties(windows-overwrite-boundary PROPERTIES WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}" TIMEOUT 60)
    endif()
endif()
target_include_directories(simnodus_resources PUBLIC src)
target_link_libraries(simnodus_resources PRIVATE simnodus::build_options)
if(WIN32)
    target_sources(simnodus_resources PRIVATE src/platform/windows/local_resources.cpp)
    target_compile_definitions(simnodus_resources PRIVATE WIN32_LEAN_AND_MEAN NOMINMAX _WIN32_WINNT=0x0A00)
    target_link_libraries(simnodus_resources PRIVATE bcrypt ntdll)
endif()
if(MSVC)
    target_compile_options(simnodus_resources PRIVATE /W4 /WX /permissive- /EHsc)
endif()
if(BUILD_TESTING)
    add_executable(native_resource_contract_tests tests/resources/native_contracts.cpp)
    target_link_libraries(native_resource_contract_tests PRIVATE simnodus_resources)
    add_test(NAME native-resource-contracts COMMAND native_resource_contract_tests)
    add_executable(native_resource_probe tests/resources/native_probe.cpp)
    target_link_libraries(native_resource_probe PRIVATE simnodus_resources)
    if(WIN32)
        add_executable(native_resource_hook_probe tests/resources/native_probe.cpp
            src/application/local_resources.cpp src/platform/windows/local_resources.cpp)
        target_include_directories(native_resource_hook_probe PRIVATE src)
        target_compile_definitions(native_resource_hook_probe PRIVATE SIMNODUS_RESOURCE_TEST_HOOKS
            WIN32_LEAN_AND_MEAN NOMINMAX _WIN32_WINNT=0x0A00)
        target_link_libraries(native_resource_hook_probe PRIVATE simnodus::build_options bcrypt ntdll)
        if(MSVC)
            target_compile_options(native_resource_hook_probe PRIVATE /W4 /WX /permissive- /EHsc)
        endif()
        add_test(NAME native-resource-filesystem
            COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/tests/resources/native_regression.py"
                --probe "$<TARGET_FILE:native_resource_probe>" --hooks "$<TARGET_FILE:native_resource_hook_probe>")
        set_tests_properties(native-resource-filesystem PROPERTIES WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}" TIMEOUT 180)
    endif()
    add_test(NAME schema-declarations
        COMMAND "${Python3_EXECUTABLE}" -m unittest discover -s tests/schema -p "test_*.py")
    set_tests_properties(schema-declarations PROPERTIES WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}")
    add_test(NAME local-resource-snapshots
        COMMAND "${Python3_EXECUTABLE}" -m unittest discover -s tests/resources -p "test_*.py")
    set_tests_properties(local-resource-snapshots PROPERTIES WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}")
    add_executable(backend_contract_tests tests/headless/backend_contracts.cpp)
    target_include_directories(backend_contract_tests PRIVATE src)
    target_link_libraries(backend_contract_tests PRIVATE simnodus::build_options)
    add_test(NAME backend-contracts COMMAND backend_contract_tests)
endif()
option(SIMNODUS_BUILD_RC_FIXTURE "Build the bounded Windows ngspice fixture runner" OFF)
if(SIMNODUS_BUILD_RC_FIXTURE)
    if(NOT WIN32 OR NOT CMAKE_SIZEOF_VOID_P EQUAL 8)
        message(FATAL_ERROR "The extracted RC fixture requires Windows x64.")
    endif()
    set(NGSPICE_ROOT "" CACHE PATH "Explicit verified Spice64_dll package")
    if(NOT EXISTS "${NGSPICE_ROOT}/include/ngspice/sharedspice.h")
        message(FATAL_ERROR "Set NGSPICE_ROOT to the verified DLL package.")
    endif()
    add_library(simnodus_rc_adapter STATIC src/adapters/ngspice/rc_fixture.cpp)
    target_include_directories(simnodus_rc_adapter PRIVATE src tools/backend_probe "${NGSPICE_ROOT}/include")
    target_compile_definitions(simnodus_rc_adapter PRIVATE WIN32_LEAN_AND_MEAN NOMINMAX)
    target_link_libraries(simnodus_rc_adapter PRIVATE simnodus::build_options)
    add_executable(simnodus_rc_fixture apps/cli/rc_fixture_main.cpp)
    target_include_directories(simnodus_rc_fixture PRIVATE src)
    target_link_libraries(simnodus_rc_fixture PRIVATE simnodus_rc_adapter)
    if(MSVC)
        target_compile_options(simnodus_rc_adapter PRIVATE /W4 /WX /permissive- /EHsc)
    endif()
endif()

# The opt-in harness uses this state gate; it does not launch engines itself.
add_executable(simnodus_session_contract apps/cli/session_contract_main.cpp)
target_include_directories(simnodus_session_contract PRIVATE src)
target_link_libraries(simnodus_session_contract PRIVATE simnodus::build_options)
if(BUILD_TESTING)
    add_executable(joint_session_tests tests/headless/joint_session.cpp)
    target_include_directories(joint_session_tests PRIVATE src)
    target_link_libraries(joint_session_tests PRIVATE simnodus::build_options)
    add_test(NAME joint-session COMMAND joint_session_tests)
endif()
if(MSVC)
    target_compile_options(simnodus_session_contract PRIVATE /W4 /WX /permissive- /EHsc)
endif()

add_library(simnodus_renode_result STATIC src/adapters/renode/cancellation_result.cpp src/adapters/renode/control_channel.cpp)
target_include_directories(simnodus_renode_result PUBLIC src)
target_link_libraries(simnodus_renode_result PRIVATE simnodus::build_options)
target_link_libraries(simnodus_session_contract PRIVATE simnodus_renode_result)
if(WIN32)
    target_compile_definitions(simnodus_renode_result PRIVATE WIN32_LEAN_AND_MEAN NOMINMAX)
endif()
if(MSVC)
    target_compile_options(simnodus_renode_result PRIVATE /W4 /WX /permissive- /EHsc)
endif()

if(BUILD_TESTING)
    add_executable(cancellation_result_tests tests/headless/cancellation_result.cpp)
    target_link_libraries(cancellation_result_tests PRIVATE simnodus_renode_result simnodus::build_options)
    add_test(NAME cancellation-result COMMAND cancellation_result_tests)
endif()

if(WIN32)
    add_executable(simnodus_fixture_process src/platform/windows/fixture_process.cpp)
    target_link_libraries(simnodus_fixture_process PRIVATE simnodus::build_options)
    target_compile_definitions(simnodus_fixture_process PRIVATE WIN32_LEAN_AND_MEAN NOMINMAX)
    if(MSVC)
        target_compile_options(simnodus_fixture_process PRIVATE /W4 /WX /permissive- /EHsc)
    endif()
endif()

target_sources(simnodus_session_contract PRIVATE src/adapters/ngspice/worker_channel.cpp)

target_sources(simnodus_session_contract PRIVATE src/adapters/ngspice/worker_reply.cpp)

if(BUILD_TESTING)
    add_executable(adc_fixture_tests tests/headless/adc_fixture.cpp)
    target_include_directories(adc_fixture_tests PRIVATE src)
    target_link_libraries(adc_fixture_tests PRIVATE simnodus::build_options)
    add_test(NAME adc-fixture COMMAND adc_fixture_tests)
endif()

target_sources(simnodus_session_contract PRIVATE src/adapters/renode/adc_process.cpp)

if(BUILD_TESTING AND WIN32)
    add_executable(adc_process_fixture tests/headless/adc_process_fixture.cpp)
    target_link_libraries(adc_process_fixture PRIVATE simnodus::build_options)
endif()

if(BUILD_TESTING)
    add_executable(rc_trajectory_tests tests/headless/rc_trajectory.cpp)
    target_include_directories(rc_trajectory_tests PRIVATE src)
    target_link_libraries(rc_trajectory_tests PRIVATE simnodus::build_options)
    add_test(NAME rc-trajectory COMMAND rc_trajectory_tests)
endif()

if(BUILD_TESTING)
    add_executable(fixture_readback_tests tests/headless/fixture_readback.cpp)
    target_include_directories(fixture_readback_tests PRIVATE src)
    target_link_libraries(fixture_readback_tests PRIVATE simnodus::build_options)
    add_test(NAME fixture-readback COMMAND fixture_readback_tests)
endif()

if(BUILD_TESTING)
    add_executable(fixture_memory_tests tests/headless/fixture_memory.cpp)
    target_include_directories(fixture_memory_tests PRIVATE src)
    target_link_libraries(fixture_memory_tests PRIVATE simnodus::build_options)
    add_test(NAME fixture-memory COMMAND fixture_memory_tests)
endif()

if(BUILD_TESTING)
    add_executable(fixture_time_tests tests/headless/fixture_time.cpp)
    target_include_directories(fixture_time_tests PRIVATE src)
    target_link_libraries(fixture_time_tests PRIVATE simnodus::build_options)
    add_test(NAME fixture-time COMMAND fixture_time_tests)
endif()

if(BUILD_TESTING)
    add_executable(fixture_registers_tests tests/headless/fixture_registers.cpp)
    target_include_directories(fixture_registers_tests PRIVATE src)
    target_link_libraries(fixture_registers_tests PRIVATE simnodus::build_options)
    add_test(NAME fixture-registers COMMAND fixture_registers_tests)
endif()

if(BUILD_TESTING)
    add_executable(fixture_inspection_tests tests/headless/fixture_inspection.cpp)
    target_include_directories(fixture_inspection_tests PRIVATE src)
    target_link_libraries(fixture_inspection_tests PRIVATE simnodus::build_options)
    add_test(NAME fixture-inspection COMMAND fixture_inspection_tests)
endif()

if(BUILD_TESTING AND WIN32)
    add_executable(fixture_adc_coordinator_tests tests/headless/fixture_adc.cpp src/adapters/renode/adc_process.cpp)
    target_include_directories(fixture_adc_coordinator_tests PRIVATE src)
    target_link_libraries(fixture_adc_coordinator_tests PRIVATE simnodus::build_options)
    add_test(NAME fixture-adc-coordinator COMMAND fixture_adc_coordinator_tests)
endif()

if(BUILD_TESTING AND WIN32)
    add_executable(fixture_analog_coordinator_tests tests/headless/fixture_analog.cpp src/adapters/ngspice/worker_channel.cpp src/adapters/ngspice/worker_reply.cpp)
    target_include_directories(fixture_analog_coordinator_tests PRIVATE src)
    target_link_libraries(fixture_analog_coordinator_tests PRIVATE simnodus::build_options)
    target_compile_definitions(fixture_analog_coordinator_tests PRIVATE WIN32_LEAN_AND_MEAN NOMINMAX)
    add_test(NAME fixture-analog-coordinator COMMAND fixture_analog_coordinator_tests)
endif()

if(BUILD_TESTING AND WIN32)
    add_executable(fixture_execution_coordinator_tests tests/headless/fixture_execution.cpp)
    target_include_directories(fixture_execution_coordinator_tests PRIVATE src)
    target_link_libraries(fixture_execution_coordinator_tests PRIVATE simnodus_renode_result simnodus::build_options)
    target_compile_definitions(fixture_execution_coordinator_tests PRIVATE WIN32_LEAN_AND_MEAN NOMINMAX)
    add_test(NAME fixture-execution-coordinator COMMAND fixture_execution_coordinator_tests)
endif()

if(BUILD_TESTING AND WIN32)
    add_executable(fixture_application_session_tests tests/headless/fixture_session.cpp src/adapters/renode/adc_process.cpp src/adapters/ngspice/worker_channel.cpp src/adapters/ngspice/worker_reply.cpp)
    target_include_directories(fixture_application_session_tests PRIVATE src)
    target_link_libraries(fixture_application_session_tests PRIVATE simnodus_renode_result simnodus::build_options)
    target_compile_definitions(fixture_application_session_tests PRIVATE WIN32_LEAN_AND_MEAN NOMINMAX)
    add_test(NAME fixture-application-session COMMAND fixture_application_session_tests)
endif()
